Application contains from server and client part.

To install dependencies, please run "yarn / npm i" command from both "server" and "client" folders

Both runs separately though "yarn start" command

For tests: "yarn test"
